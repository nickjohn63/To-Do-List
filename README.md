# 5-Tab To-Do – iPhone Wrap Fix Package

Drop these files into your GitHub repo (replace existing), commit to `main`, and GitHub Pages will serve the updated build.

Changes:
- CSS fix for iPhone one-word-per-line (flexbox min-width:0, scoped wrapping)
- Kept original look and spacing
- Cache name bumped to v6 so phones fetch fresh CSS

Files:
- index.html
- manifest.webmanifest
- service-worker.js
- icons/icon-192.png
- icons/icon-512.png
